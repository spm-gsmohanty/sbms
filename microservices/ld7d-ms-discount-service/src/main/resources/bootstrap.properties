spring.application.name=discount-service
# The external configuration from config server overrides the local configuration
spring.cloud.config.uri=http://localhost:8888
#default profile loaded by default
spring.profiles.active=dev